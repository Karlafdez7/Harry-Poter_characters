import FilterName from "./FilterName";
import FilterHouse from "./FilterHouse";

const Filters = () =>{
    return (
        <section>
            <form>
                <FilterName/>
                <FilterHouse/>
            </form>
        </section>
    );
}

export default Filters